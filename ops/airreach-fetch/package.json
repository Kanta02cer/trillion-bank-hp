{
  "name": "trillion-bank-airreach-fetch",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types && tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.129.0"
  }
}
